fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
SQLAlchemy>=2.0
# NOTE: passlib's bcrypt handler is not compatible with newer bcrypt releases
# (and bcrypt enforces a 72-byte password limit). Timeboard uses PBKDF2-SHA256
# to avoid that dependency and keep the image slim and reliable.
passlib>=1.7
python-jose[cryptography]>=3.3
PyYAML>=6.0
APScheduler>=3.10
itsdangerous>=2.1
